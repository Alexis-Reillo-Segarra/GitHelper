// Paleta de la TUI (morado sobre negro). Ink acepta hex en las props `color`.
export const colors = {
    purple: "#8b5cf6",
    purpleLight: "#a78bfa",
    dim: "#6b7280",
    gray: "#9ca3af",
    fg: "#e5e7eb",
    ok: "#22c55e",
    warn: "#f59e0b",
    bad: "#ef4444",
    sky: "#38bdf8",
} as const;

// Mascota compacta para la cabecera de la TUI (pulpo con casco).
export const OCTOPUS_MINI = ["  ___", " /===\\", "(o   o)", " \\_~_/"];
